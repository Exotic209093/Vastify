node_modules/
dist/
build/
.env
.env.local
.env.*.local
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
Thumbs.db

# SQLite (keep schema.sql, ignore data)
*.db
*.db-wal
*.db-shm
!api/src/db/schema.sql

# Salesforce
salesforce/.sf/
salesforce/.sfdx/
salesforce/.localdevserver/
salesforce/deploy-options.json
.sf/
.sfdx/

# VS Code / editors
.vscode/*
!.vscode/settings.json
.idea/

# MinIO / docker volume data
.minio-data/

# Dashboard
dashboard/dist/
dashboard/.vite/
dashboard/vite.config.d.ts
dashboard/vite.config.js
dashboard/tsconfig.tsbuildinfo
dashboard/tsconfig.node.tsbuildinfo
*.tsbuildinfo

# API static build output
api/public/

# Local secrets (never commit)
.env.docker
DEPLOY_NOTES.md

# Git worktrees
.worktrees/
.worktrees

# Playwright MCP cache (browser snapshots)
.playwright-mcp/

# Demo video / hyperframe generation artifacts (heavy, regenerable)
# The whole hyperframes/ build output is too big for git; live link in docs/submission/README.md.
docs/submission/hyperframes/
frame-*.png
frame-01-preview.png
*.mp4
*.wav
*.mp3

# Claude Code session data (per-machine)
.claude/

# Generated marketing site bundle (was a one-off export, regenerable)
Vastify Site.html
